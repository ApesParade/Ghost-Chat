services:
  - type: web
    name: ghost-chat
    env: node
    buildCommand: npm install && npx prisma generate && npx prisma db push
    startCommand: node dist/index.js
    envVars:
      - key: NODE_VERSION
        value: 20.18.0
    plan: free